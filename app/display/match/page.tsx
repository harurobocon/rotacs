import { MatchDisplayComponent } from "@/components/display/match-display";

export const metadata = {
  title: "試合呼び出し画面 - RoTACS",
  description: "大会当日の試合進行およびチーム呼び出し案内画面",
};

export default function MatchDisplayPage() {
  return <MatchDisplayComponent />;
}
